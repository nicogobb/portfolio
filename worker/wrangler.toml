name            = "cover-letter-worker"
main            = "index.js"
compatibility_date = "2024-01-01"

# Secrets are set via CLI — never put real values here:
#   wrangler secret put LETTER_API_KEY
#   wrangler secret put AUTH_TOKEN
